enum Pages {
  Home,
  Activity,
  MyContact,
  Notification,
  Menu,
  Budget,
}

enum AdsFilter {
  Brands,
  Normal,
}

enum AdvertiserPage{
  Home,
  Insight,
  Editors
}

enum Interact { Like, Dislike }
enum OwnerPages { Home, Insight, Notification, Settings }

enum stepType { TEXT,DESCRIPTION, NUMBER,PORTION, MEDIA, LIST }

enum durationType { OPEN_TIME , CLOSE_TIME }
